// to install packages, we have to initialize our project with 
// npm init, npm init -y

// to install packages like express, tailwind etc
// npm i package name
// npm i express

// to install a package globally for all the files, nodemon restarts sever on every change, you do not need to restart it manually
// npm i -g nodemon
// npm i -global nodemon

// to run a backend js file
// node server.js
// nodemon server.js

// nvm package used to work with different node versions
// nvm(node version manager)

